# .github/workflows/ci.yml

# .github/workflows/ci.yml

# .github/workflows/html-check.yml

name: HTML Check

on:
  push:
    branches: [main, feature3]

jobs:
  html-validation:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v3

      - name: Run HTML validator
        uses: Cyb3r-Jak3/html5validator-action@v1.2.1
